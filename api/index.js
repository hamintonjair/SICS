// Este es un archivo de marcador de posición para la API
// En un entorno de producción real, deberías implementar las rutas de tu API aquí
// o usar un servicio externo para tu backend.

export default async function handler(req, res) {
  res.status(200).json({ message: 'API en desarrollo' });
}
